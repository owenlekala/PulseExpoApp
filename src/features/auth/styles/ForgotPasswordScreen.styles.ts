import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  scrollView: {
    flex: 1,
  },
  scrollContent: {
    padding: 24,
    paddingBottom: 40,
  },
  container: {
    paddingTop: 60,
  },
  title: {
    fontSize: 32,
    fontWeight: '700',
    marginBottom: 8,
    textAlign: 'left',
  },
  subtitle: {
    fontSize: 16,
    marginBottom: 32,
    textAlign: 'left',
  },
  message: {
    fontSize: 16,
    marginBottom: 32,
    textAlign: 'left',
  },
  errorText: {
    fontSize: 14,
    marginTop: 8,
  },
});

